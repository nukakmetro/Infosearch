# Infosearch
Python 3.14 Открыть через pycharm проект -> запустить

1. Создан инвертированный список терминов (индекс)
Был реализован скрипт, который проходит по всем файлам с леммами документов (документы 1–100) и строит инвертированный индекс.
Для каждого термина формируется список документов, в которых он встречается.

Результат сохраняется в файл invert_list.json в формате:
